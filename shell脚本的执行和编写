shell脚本的执行和编写
用途：完成特定的较复杂的系统管理任务
格式：集中保存多条命令 普通文本文件
执行方式：按照预设的顺序依次解释执行

建立包含执行语句的脚本文件
脚本文件中包含的内容
  第一行是 指明解析器的位置
  #!/bin/bash 脚本文件的运行环境设置 表明脚本依赖的解析器 找到解析器的位置
  之后的＃都代表注释
  #
  #
  这样的脚本文件 只要添加了执行权限 就无需 bash file.sh（不需要写解析器  不需要给脚本添加执行权限）来执行 ；可以直接执行（需要写解析器 需要给脚本添加执行权限）